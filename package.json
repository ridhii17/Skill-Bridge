{
  "name": "skillbridge-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Smart Competency Diagnostic and Candidate Profile Score Calculator — SIH1628",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "seed": "cd server && node src/seeders/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
